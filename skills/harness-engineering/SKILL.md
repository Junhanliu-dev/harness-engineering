---
name: harness-engineering
description: Analyze any existing codebase, discover its patterns and best practices, and generate a Harness Engineering structure (rules, skills, wiki, pipeline) tailored to that project's language and conventions
---

# Harness Engineering

Discover the actual patterns, conventions, and architecture of an existing codebase, then generate a structured constraint system that ensures AI agents produce code matching those standards.

## When to Use

- "Set up harness for this project"
- "Create harness structure for my codebase"
- "Make AI code production-ready for this repo"
- "Build agent constraints for this project"
- "Initialize harness engineering"

## Philosophy

> When an agent makes an error, engineer its elimination — not with prompt tweaks, but with files, rules, automated checks, and system structure.

**Core insight:** The problem isn't model intelligence. It's that models don't know the unwritten rules — patterns every experienced developer on the team knows but nobody documented.

This skill **discovers** those rules from the code itself, then encodes them as machine-enforceable constraints.

## File Layout of This Skill

```
harness-engineering/
├── SKILL.md                       # this file — overview + phase index
├── templates/                     # markdown templates emitted into target project
│   ├── rules/                     # → harness/rules/ in target
│   ├── skills/                    # → harness/skills/<name>/SKILL.md
│   ├── agents/                    # → harness/agents/ in target
│   ├── agent.md                   # → harness/agent.md (orchestrator)
│   └── pipeline.md                # → harness/pipeline.md
├── hook-templates/                # shell scripts emitted into harness/hooks/
└── references/                    # deep-dive content read on demand
    ├── discovery-checklist.md     # Phase 1 detail
    ├── wiring.md                  # Phase 10 detail
    ├── validation.md              # Phase 11 detail
    └── wiki-templates.md          # Phase 6 wiki stubs
```

**Rule:** Every phase below tells you which template/reference to read. Open them with the Read tool when the phase fires — do NOT invent template content from memory.

## Output Structure in Target Project (after full setup)

```
project-root/
├── .claude/
│   ├── rules/                     # symlinks to harness/rules/*.md
│   ├── skills/                    # symlinks to harness/skills/*
│   ├── agents/                    # symlinks to harness/agents/*.md
│   └── settings.json              # hooks for quality gates
├── CLAUDE.md                       # references harness/agent.md
├── harness/
│   ├── agent.md                    # orchestrator definition
│   ├── rules/                      # engineering-structure, coding-standards, development-process
│   ├── skills/                     # folder name MUST equal SKILL.md `name:` frontmatter
│   │   ├── harness-coding/
│   │   │   ├── SKILL.md
│   │   │   └── specs/{layer}.md
│   │   ├── harness-review/SKILL.md
│   │   ├── harness-testing/SKILL.md
│   │   ├── harness-requirements/SKILL.md
│   │   └── harness-run/SKILL.md
│   ├── agents/                     # harness-coder.md, harness-reviewer.md
│   ├── wiki/                       # architecture, data-models, critical-paths, external-services
│   ├── hooks/                      # check-layer-boundaries.sh, pre-push-gate.sh
│   ├── pipeline.md
│   └── changes/_template/          # requirements.md, task-breakdown.md, coding-report.md, review-record.md, pipeline-state.md, ci-result.md
└── src/  (existing code)
```

---

## Skill Naming Invariant (CRITICAL — read before creating any skill)

Claude Code's skill loader compares the **folder name** against the SKILL.md `name:` frontmatter. If they differ, the skill emits a warning and may fail to register.

**Rule:** every skill folder MUST be named identically to its `name:` frontmatter value.

```
✅ CORRECT
harness/skills/harness-coding/SKILL.md         (name: harness-coding)
harness/skills/harness-review/SKILL.md         (name: harness-review)

❌ WRONG (will warn / break)
harness/skills/coding/SKILL.md                 (name: harness-coding)
harness/skills/review/SKILL.md                 (name: harness-review)
```

When you generate a skill:
1. Choose the `name:` value first (must be globally unique, kebab-case, descriptive — prefix with `harness-` for skills inside this harness).
2. Use the SAME string as the folder name.
3. Symlinks in `.claude/skills/<name>` then resolve to a same-named source folder — no mismatch possible.

This applies to **all** skill folders generated by this skill: harness-coding, harness-review, harness-testing, harness-requirements, harness-run, and any additional ones added later.

---

## Phase Dependency Note

Phases are not independent. Later phases produce files that earlier-phase outputs reference. In particular, **Phase 3's `harness-run` skill** references files from **Phases 4, 5, and 7** (`harness/agent.md`, `harness/pipeline.md`, `harness/agents/harness-coder.md`, `harness/agents/harness-reviewer.md`). If you stop after Phase 3, those refs dangle until the later phases run.

**Rule:** run all 11 phases in order for a complete, internally consistent harness. Partial runs are fine for iteration but expect dangling refs to outputs from skipped phases.

---

## Phase 1: Discovery (Language & Stack Detection)

Understand what this project IS before prescribing anything.

**Read first:** `references/discovery-checklist.md` — full detection steps (language, architecture, coding patterns, testing, CI, unwritten rules, best practices).

Output of this phase: notes on language, framework, layers, conventions, test framework, CI checks, and unwritten rules — used by every later phase.

---

## Phase 2: Generate Rules

Rules are **stable constraints that don't change per-requirement**. Derive them entirely from Phase 1 findings.

Generate into `harness/rules/` using these templates:

| Target file | Template |
|-------------|----------|
| `harness/rules/engineering-structure.md` | `templates/rules/engineering-structure.md` |
| `harness/rules/coding-standards.md`      | `templates/rules/coding-standards.md` |
| `harness/rules/development-process.md`   | `templates/rules/development-process.md` |

For each: Read the template, substitute `{placeholders}` with Phase 1 findings, write to target.

---

## Phase 3: Generate Skills

Create one folder per skill under `harness/skills/<name>/`, each containing a `SKILL.md` with the SAME `name:` as its folder.

| Target | Template |
|--------|----------|
| `harness/skills/harness-coding/SKILL.md`      | `templates/skills/harness-coding.md` |
| `harness/skills/harness-coding/specs/{layer}.md` (one per non-trivial layer — see below) | `templates/skills/harness-coding-spec.md` |
| `harness/skills/harness-review/SKILL.md`      | `templates/skills/harness-review.md` |
| `harness/skills/harness-testing/SKILL.md`     | `templates/skills/harness-testing.md` |
| `harness/skills/harness-requirements/SKILL.md`| `templates/skills/harness-requirements.md` |
| `harness/skills/harness-run/SKILL.md`         | `templates/skills/harness-run.md` |

### When to create a per-layer spec

Create a spec for a layer if **any** of these are true:
- It has a non-trivial file template (e.g., a Rails controller, a React route component) that new files must follow.
- It has import/dependency rules that differ from sibling layers.
- It has layer-specific anti-patterns worth documenting.

Skip the spec for trivial wrapper layers (e.g., a thin `index.ts` barrel directory) — the main `harness-coding/SKILL.md` is sufficient.

Each spec is derived from reading 2-3 real files in that layer and extracting the common template, import rules, patterns, and anti-patterns.

Note: the spec template (`templates/skills/harness-coding-spec.md`) intentionally has **no frontmatter** — specs are sub-pages of `harness-coding`, not standalone registered skills.

---

## Phase 4: Generate Orchestrator Agent

**Target:** `harness/agent.md`
**Template:** `templates/agent.md`

This file is referenced from CLAUDE.md and tells the main agent how to operate the harness.

---

## Phase 5: Generate Pipeline

**Target:** `harness/pipeline.md`
**Template:** `templates/pipeline.md`

The pipeline defines 10 stages with gates, rollback rules, and review cycle limits. The `harness-run` skill orchestrates execution of these stages.

---

## Phase 6: Generate Wiki

Wiki is **on-demand context** — agent queries it when needed, not loaded by default.

**Read first:** `references/wiki-templates.md` — stubs for architecture, data-models, critical-paths, external-services.

Populate from what's discoverable in code.

---

## Phase 7: Generate Sub-Agent Definitions

Sub-agents are the mechanism for **separating execution from judgment**. They receive isolated context and can only see what's loaded into their prompt.

| Target | Template |
|--------|----------|
| `harness/agents/harness-coder.md`    | `templates/agents/harness-coder.md` |
| `harness/agents/harness-reviewer.md` | `templates/agents/harness-reviewer.md` |

---

## Phase 8: Generate Hooks (Language-Specific)

Hooks provide **programmatic enforcement** — they run automatically and can block actions.

Pick the right boundary-check template by detected language and copy to `harness/hooks/check-layer-boundaries.sh`. Replace the `case` patterns with the project's REAL directory names from Phase 1.

| Language | Template |
|----------|----------|
| TypeScript / JavaScript | `hook-templates/check-layer-boundaries-typescript.sh` |
| Go                      | `hook-templates/check-layer-boundaries-go.sh` |
| Python                  | `hook-templates/check-layer-boundaries-python.sh` |

Always copy:
- `hook-templates/pre-push-gate.sh` → `harness/hooks/pre-push-gate.sh` (replace `{build_command}`, `{lint_command}`, `{test_command}` with Phase 1.5 commands)
- `hook-templates/post-edit-wrapper.sh` → `harness/hooks/post-edit-wrapper.sh` (no substitutions)

Then `chmod +x` all hooks (see Phase 10).

---

## Phase 9: Pipeline Orchestrator Skill

Already covered in Phase 3 — the `harness-run` skill (template: `templates/skills/harness-run.md`) is what drives the pipeline. Listed separately here because it's the entry point users invoke (`/harness-run <requirement>`).

---

## Phase 10: Wiring (Connect Harness to Claude Code Runtime)

**This is the critical phase.** Generated files are inert until wired into the execution environment.

**Read first:** `references/wiring.md` — full wiring steps: symlinks for rules/skills/agents, CLAUDE.md append, `.claude/settings.json` hook config, change-tracking template, chmod.

Key point: hook commands in `.claude/settings.json` MUST use `$CLAUDE_PROJECT_DIR` for absolute paths — relative paths fail at fire time.

---

## Phase 11: Validation (Dry Run)

After generation and wiring, validate end-to-end.

**Read first:** `references/validation.md` — 11-row verification checklist + cross-reference checks + hook tests + session-resumption test.

Report: "Harness validated. {N} checks passed, {M} issues found: {list}"

---

## Key Principles

### 1. Discover, Don't Prescribe
Read the code. Extract patterns. Don't impose templates from other projects.

### 2. Quality Gates Must Be Programmatic
```
BAD:  "Check if CI passes"
GOOD: "ci_status == 'success' AND total_tests > 0 AND tests_passed == total_tests"
```
If a constraint can't be machine-verified, the agent WILL drift from it.

### 3. Separate Execution from Judgment
The agent that writes code NEVER reviews its own code. Different `.claude/agents/` with different tool sets (coder has Write/Edit; reviewer has only Read).

### 4. Context Layering
| Layer | Content | Mechanism | When |
|-------|---------|-----------|------|
| Always | rules/ | `.claude/rules/` symlinks | Every session |
| Stage | skills/ | `/harness-*` invocation | During that phase |
| Delegated | agents/ | Agent tool prompt | Sub-agent scope |
| On-demand | wiki/ | Read tool | Agent queries as needed |

### 5. Every Rule Has a Reason
Don't add rules speculatively. Each rule should either:
- Reflect an observed consistent pattern in the codebase, OR
- Prevent a known failure mode

### 6. Living System
After each real task:
1. Did the agent make a new kind of error? → Add rule/constraint
2. Is a rule blocking valid code? → Remove or refine it
3. Did the pipeline get stuck? → Adjust stage gates or limits

---

## Quick Start (Minimum Viable Harness)

If the full structure is too much, start with 3 files + wiring:

```bash
# 1. Create minimum harness
mkdir -p harness
# Write coding-standards.md (Phase 2 output)
# Write review-skill.md (Phase 3 review output)
# Write ci-gates.md (programmatic conditions)

# 2. Wire it
mkdir -p .claude/rules
ln -sf "$(pwd)/harness/coding-standards.md" .claude/rules/harness-standards.md

# 3. Reference in CLAUDE.md
cat >> CLAUDE.md << 'EOF'

## Harness (Minimum)
Before writing code: Read harness/coding-standards.md
Before marking done: Run review checklist in harness/review-skill.md
Before pushing: Verify conditions in harness/ci-gates.md
EOF
```

This alone reduces rework cycles from 3-5 rounds to typically 1.
